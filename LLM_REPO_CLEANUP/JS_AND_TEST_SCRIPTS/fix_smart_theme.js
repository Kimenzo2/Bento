const fs = require('fs'); let code = fs.readFileSync('components/SmartEditor.tsx', 'utf8'); code = code.replace(/bg-cream-base/g, 'bg-cream-base dark:bg-slate-950'); code = code.replace(/bg-surface/g, 'bg-surface dark:bg-slate-900'); code = code.replace(/text-charcoal-soft/g, 'text-charcoal-soft dark:text-slate-200'); code = code.replace(/text-cocoa-light/g, 'text-cocoa-light dark:text-slate-400'); fs.writeFileSync('components/SmartEditor.tsx', code);
