{
  "version": 2,
  "builds": [
    {
      "src": "src/index.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [{ "src": "/api/(.*)", "dest": "src/index.js" }],
  "env": {
    "OPENAI_API_KEY": "@openai-api-key",
    "JWT_SECRET": "@jwt-secret"
  }
}
